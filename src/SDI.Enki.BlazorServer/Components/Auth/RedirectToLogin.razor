@* Fired from the <NotAuthorized> slot on AuthorizeRouteView. Force-navigates
   to the Identity challenge endpoint with a returnUrl so the user lands back
   on the protected page after login. forceLoad:true is required — the target
   is a server-side Minimal API endpoint, not a Blazor route. *@
