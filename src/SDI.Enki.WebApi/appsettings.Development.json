{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "_comment": "Master DB connection for tenant-registry lookups. Per-tenant connection strings are resolved at request time from the TenantDatabase table in master.",
    "Master": "Server=(localdb)\\MSSQLLocalDB;Database=Enki_Master;Trusted_Connection=True;TrustServerCertificate=True;"
  }
}
