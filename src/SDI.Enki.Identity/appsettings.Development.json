{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.Hosting.Lifetime": "Information",
        "Microsoft.EntityFrameworkCore": "Warning"
      }
    }
  },
  "ConnectionStrings": {
    "_comment": "Local SQL Server (default instance) with SQL auth. Identity DB holds AspNetUsers / OpenIddict tables — separate from master and tenant DBs.",
    "Identity": "Server=localhost;Database=Enki_Identity;User Id=sa;Password=5q!@dm1n1str@t0r;TrustServerCertificate=True;Encrypt=True;"
  }
}
