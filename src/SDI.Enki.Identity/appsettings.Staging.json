{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.Hosting.Lifetime": "Information",
        "Microsoft.EntityFrameworkCore": "Warning"
      }
    }
  },
  "Identity": {
    "_comment": "Path to the OIDC signing/encryption PFX. Required outside Development — the OpenIddict registration in Program.cs throws if missing. Password lives in the Identity__SigningCertificate__Password env var (kept out of source control).",
    "SigningCertificate": {
      "Path": "C:\\Enki\\dev\\certs\\enki-identity-signing.pfx"
    }
  }
}
