{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "SessionLifetime": {
    "_comment": "Token lifetime defaults. Per-user overrides on ApplicationUser.SessionLifetimeMinutes win up to MaxRefreshTokenLifetimeMinutes; the admin endpoint and the OpenIddict per-request handler both clamp to that ceiling.",
    "AccessTokenLifetimeMinutes":     60,
    "RefreshTokenLifetimeMinutes":    14400,
    "MaxRefreshTokenLifetimeMinutes": 525600
  }
}
